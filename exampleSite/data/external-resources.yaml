# External resources are refreshed only when explicitly running:
#   npm run prepare:external
# Example consumer sites can declare remote files here when content needs a
# checked-in local copy before Hugo builds.
resources: []
